for (let index = 1; index < 11; index++){
		let multiples = 1;
		let sums = 0;
		let multiple = 1;
		let sum = 0;
		for(let  times = 0; times < 5; times++){
		if(index % 4 == 0){
					}
					multiples *= 4;
					sums += multiples;
						}
		
		for(let time = 0; time < 5; time++){
		if(index % 8 == 0){
				}
			multiple *= 8;
			sum += multiple;
				}
		let summation = sum + sums;
		console.log(summation * summation);
		console.log( " " );
	
		break;
	}

	







